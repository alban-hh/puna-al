# Base URL of the Puna.al backend API, including the version prefix.
# The backend serves everything under /api/v1 (see API.md §1).
# Local dev default (backend binds 0.0.0.0:8080):
VITE_API_BASE_URL=http://localhost:8080/api/v1
